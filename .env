# Local development — direct URL to the hospital-service
VITE_HOSPITAL_SERVICE_URL=http://localhost:8100

# VITE_HOSPITAL_PROXY_TARGET is intentionally unset here.
# It is injected by docker-compose so the Vite proxy forwards /api/* to
# the internal Docker service name instead of localhost.